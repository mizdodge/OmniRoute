# Project Status

## Feature: Adaptive Smart Routing

### Phase 1 — Architecture Validation & Implementation Blueprint

Status: Completed

#### Repository

- **Branch**: `release/v3.8.50` (HEAD: b754e44e2)
- **Version**: 3.8.50
- **Package Manager**: pnpm (workspaces: `open-sse`, `packages/browser-pool`)
- **Test Setup**: Node.js native test runner (`node --import tsx/esm --test`) + Vitest (MCP server, autoCombo, cache)
- **Coverage Gate**: 60% statements/lines/functions/branches (ratchet in `quality-baseline.json`)

#### Combo Builder Flow

The Combo Builder is a 5-stage wizard:

1. **Basics** — Name, description, strategy selection
2. **Steps** — Add model steps (provider/model/connection), combo-refs, provider-wildcards
3. **Strategy** — Select routing strategy (priority, weighted, round-robin, auto, lkgp, etc.)
4. **Intelligent** — Only shown for `auto` or `lkgp` strategies; configures intelligent routing
5. **Review** — Summary before save

**Stage gating**: `getComboBuilderStages()` returns all 5 stages for intelligent strategies, 4 stages otherwise. `canAccessComboBuilderStage()` enforces sequential completion.

**Draft state**: Stored in React state (`name`, `models[]`, `strategy`, `config`, `showAdvanced`, etc.) in `page.tsx`. The `config` object holds the intelligent routing configuration.

**Step storage**: `models` array holds `ComboBuilderDraftModelStep[]` — each entry has `{ model: string, providerId?: string, weight: number }`. These are normalized to `ComboStep[]` via `normalizeComboModels()` on save.

**Strategy storage**: Single string in `strategy` state (e.g., "priority", "auto", "lkgp").

**Intelligent Routing config**: Stored in `config` object, normalized via `normalizeIntelligentRoutingConfig()`. Contains:

- `candidatePool: string[]` — provider IDs to evaluate
- `explorationRate: number` — 0-1
- `modePack: string` — "ship-fast", "cost-saver", "quality-first", "offline-friendly"
- `budgetCap?: number`
- `weights: IntelligentRoutingWeights` — 13 factors
- `routerStrategy: string` — "rules", "cost", "latency", "sla-aware", "lkgp"
- `slaTargetP95Ms?, slaMaxErrorRate?, slaMaxCostPer1MTokens?, slaHardConstraints`

**Serialization**: On save, `normalizeComboModels()` converts draft steps to `ComboStep[]` with stable `id` fields. The full combo record (including `config`) is persisted via `createCombo()`/`updateCombo()` to the `combos` table (JSON in `data` column).

**Persistence**: `combos` table has `id`, `name`, `data` (JSON), `created_at`, `updated_at`. The `data` column stores the full combo including `models: ComboStep[]`, `strategy`, `config`, `version: 2`.

#### Combo Step Architecture

**`ComboModelStep`** (from `src/lib/combos/steps.ts`):

```typescript
{
  id: string;                    // Stable: "model-comboName-1-provider/model:connectionId"
  kind: "model";
  model: string;                 // Qualified: "provider/model"
  providerId?: string | null;    // Canonical provider ID
  connectionId?: string | null;  // Pinned connection, or null for auto
  allowedConnectionIds?: string[] | null;  // Account allowlist (#3266)
  weight: number;                // 0-100
  label?: string;                // Optional display label
  prompt?: string | null;        // Per-step pipeline input
  tags?: string[];               // Optional tags
  fallbackOnlyOnQuotaExhaustion?: boolean;
}
```

**Stable identifier**: The `id` field is generated by `buildStepId()` using `kind`, `comboName`, `index`, and a seed (model string + connectionId). This survives UI reordering but changes if the step is deleted and re-added.

**Signature for deduplication**: `getExactModelStepSignature()` produces `model:providerId:modelId:connectionId` (or `__auto__`). Used by `hasExactModelStepDuplicate()` to prevent duplicate steps.

**Combo-ref behavior**: Steps with `kind: "combo-ref"` reference another combo by name. Expanded at runtime via `resolveNestedComboTargets()`.

**Duplicate handling**: Exact duplicates (same provider/model/connection) are prevented at add-time. Different connections for same provider/model are allowed as separate steps.

#### Intelligent Routing Architecture

**`IntelligentRoutingConfig`** (from `src/lib/combos/intelligentRouting.ts`):

- `candidatePool: string[]` — Provider IDs to consider (empty = all active)
- `explorationRate: number` — 0-1, default 0.05
- `modePack: string` — One of 4 presets, default "ship-fast"
- `budgetCap?: number` — USD per request
- `weights: IntelligentRoutingWeights` — 13 factors summing to ~1.0
- `routerStrategy: string` — "rules" | "cost" | "latency" | "sla-aware" | "lkgp"
- `slaTargetP95Ms?, slaMaxErrorRate?, slaMaxCostPer1MTokens?, slaHardConstraints`

**Candidate Pool**: Built from active providers in `BuilderIntelligentStep.tsx` via `toProviderOptions()`. Can be restricted by user selection.

**Scoring Weights** (DEFAULT_INTELLIGENT_WEIGHTS):

- quota: 0.16, health: 0.20, costInv: 0.16, latencyInv: 0.12
- taskFit: 0.08, stability: 0.05, tierPriority: 0.05, tierAffinity: 0.05
- specificityMatch: 0.05, contextAffinity: 0.08, cacheAffinity: 0
- sessionAvailability: 0.05, resetWindowAffinity: 0

**Mode Packs**: 4 presets in `MODE_PACK_OPTIONS` that bias weights.

**Router Strategies**: 5 options in `ROUTER_STRATEGY_OPTIONS`.

**Runtime consumption**:

- Virtual auto-combos use `createVirtualAutoCombo()` → `createVirtualAutoComboFromPrepared()` which builds `autoConfig` with `candidatePool`, `weights`, `explorationRate`, `routerStrategy`.
- Combo routing uses `scoreAutoTargets()` in `autoStrategy.ts` which calls `calculateScore()` from `scoring.ts`.
- The `IntelligentRoutingConfig` is stored in combo's `config` field and also mirrored in `autoConfig` for auto-combos.

#### Current Task Fit

**Source**: `getTaskFitness()` from `open-sse/services/autoCombo/taskFitness.ts`

**Resolution chain** (highest → lowest priority):

1. User override — DB `model_intelligence` where `source='user_override'`
2. Arena ELO — DB `model_intelligence` where `source='arena_elo'`
3. Models.dev tier — Derived from `model_capabilities` table (reasoning/tool_call/context)
4. Static FITNESS_TABLE — Hardcoded lookup (model pattern × task type)
5. Wildcard boosts — Pattern matching (e.g., "coder" → +0.15 coding)

**Task types**: coding, review, planning, analysis, debugging, documentation, default

**Integration**: Used in `calculateFactors()` → `factors.taskFit` → `weights.taskFit * factors.taskFit` in scoring.

**Prompt analysis**: No automatic prompt analysis currently. Task type must be explicitly provided or inferred from routing hints.

#### Model Option Source

For Fast Worker / Strong Reasoning selectors, the options MUST come from **this Combo's Steps**.

**Cleanest source**: The `models` state in `page.tsx` (already normalized `ComboBuilderDraftModelStep[]`). Each entry has `model` (qualified "provider/model") and `providerId`.

**Transformation helper**: `getComboModelString()` from `steps.ts` extracts the qualified model string. The `id` field from normalized `ComboStep` provides the stable reference.

**Recommended approach**: Map `models` array to dropdown options:

```typescript
models.map((step, index) => ({
  value: step.id, // Stable step ID
  label: `${step.model} (${step.connectionId ? "pinned" : "auto"})`,
  providerId: step.providerId,
  modelId: step.model.split("/")[1],
}));
```

#### Future Fast Worker Pool

- **Purpose**: Preferred for low-complexity/high-volume tasks (simple Q&A, formatting, basic coding)
- **Optimization**: Speed/cost
- **Selection**: User picks **one or more** models from existing Combo Steps
- **Runtime behavior**: Acts as a routing pool for "simple" task types; existing Intelligent Routing scorer selects best eligible model within the pool

#### Future Strong Reasoning Pool

- **Purpose**: Preferred for difficult reasoning-heavy tasks (complex coding, debugging, planning, analysis)
- **Optimization**: Quality/reasoning capability
- **Selection**: User picks **one or more** models from existing Combo Steps
- **Runtime behavior**: Acts as a routing pool for "complex" task types; existing Intelligent Routing scorer selects best eligible model within the pool

#### Persistence Strategy

**Location**: Add to `IntelligentRoutingConfig` (stored in combo's `config` field):

```typescript
interface IntelligentRoutingConfig {
  // ... existing fields
  fastWorkerModelRefs?: string[]; // Array of Step ID references
  strongReasoningModelRefs?: string[]; // Array of Step ID references
}
```

**Normalization**: In `normalizeIntelligentRoutingConfig()`, validate that referenced step IDs exist in the combo's current steps.

**Missing references**: If a referenced step is deleted, ignore that reference (fall back to remaining pool members or normal scoring).

**Duplicate models on different connections**: Distinguished by step `id` (includes connectionId in seed). Each pool entry is a distinct step reference.

#### Runtime Integration Point

**Where scoring happens**: `scoreAutoTargets()` in `open-sse/services/combo/autoStrategy.ts` → `calculateScore()` in `open-sse/services/autoCombo/scoring.ts`.

**Extension point**: Add role pool suitability factors to `ScoringFactors`:

```typescript
interface ScoringFactors {
  // ... existing
  fastWorkerPoolSuitability?: number; // 1.0 if candidate in Fast Worker pool
  strongReasoningPoolSuitability?: number; // 1.0 if candidate in Strong Reasoning pool
}
```

**Weight integration**: Add `fastWorkerPoolWeight` and `strongReasoningPoolWeight` to `ScoringWeights` (default 0, configurable via mode packs or explicit weights).

**Task classification**: Need lightweight heuristic to classify request as "simple" vs "complex" → maps to preferring Fast Worker pool vs Strong Reasoning pool.

#### UI Integration Point

**Component**: `BuilderIntelligentStep.tsx` — extends the existing Intelligent Routing step.

**Addition**: After "Advanced: Scoring Weights" section, add:

```
Fast Worker Pool
[Multi-select: Models from Combo Steps ▼]

Strong Reasoning Pool
[Multi-select: Models from Combo Steps ▼]
```

**State update**: Via existing `onChange` callback that merges into `config`.

**Model options**: Derived from `models` prop (passed from `page.tsx`) — the current Combo Steps.

#### Candidate Pool Interaction

1. **Both role pools guaranteed candidates?** Yes — they come from Combo Steps which are already in the candidate pool (or can be added to it).
2. **Other Combo Step models still participate?** Yes — models not in either pool still participate in normal scoring.
3. **Adaptive scoring ranks all Combo Steps?** Yes — all steps scored; role pool membership adds suitability factor.
4. **Role pool membership = score boost?** Yes — not a hard filter; existing Intelligent Routing scorer selects best within pool.
5. **Only one role pool configured?** Works — only that pool's suitability applies.
6. **Same step in both pools?** Allowed — model can serve both roles; suitability factors apply independently.
7. **Role references removed step?** Ignored at runtime (validation on save warns, but runtime degrades gracefully).
8. **Intra-role failover?** Yes — if preferred pool candidate fails, existing scorer selects next best from same pool.
9. **Cross-role failover?** Yes — if entire preferred pool exhausted, fall back to other role pool, then existing Combo fallback.
10. **Empty pools?** Handled safely — empty pool treated as no preference; falls back to normal scoring.

#### Adaptive Analysis Design Direction

**Signals to evaluate** (lightweight, deterministic first):

- Message count / token estimate → complexity proxy
- Tool count / tool types → tool complexity
- Presence of code blocks, stack traces → coding/debugging
- Request length, nested reasoning → reasoning complexity
- Context window utilization → context complexity

**Where to run**: In `handleChatCore()` or `handleSingleModelChat()` before scoring — have access to `body`, `model`, `provider`.

**Latency target**: <5ms (synchronous, no LLM calls).

**Feeding scoring**: Output `taskType` ("simple" | "complex" | "coding" | "debugging" | "planning" | "analysis") → maps to role preference.

**Failure degradation**: Default to "simple" (prefer Fast Worker) or neutral (no boost).

#### Scoring Integration Direction

**Conceptual**:

```
Final Score = Existing Intelligent Score + (Role Pool Suitability × Role Pool Weight)
```

**Implementation**:

1. Add `fastWorkerPoolSuitability`, `strongReasoningPoolSuitability` to `ScoringFactors`
2. Add `fastWorkerPoolWeight`, `strongReasoningPoolWeight` to `ScoringWeights` (default 0)
3. In `calculateFactors()` / `scoreAutoTargets()`, compute suitability:
   - If candidate.stepId in config.fastWorkerModelRefs → fastWorkerPoolSuitability = 1.0
   - If candidate.stepId in config.strongReasoningModelRefs → strongReasoningPoolSuitability = 1.0
   - Else 0
4. Task classification determines which role pool weight is active:
   - Simple task → apply fastWorkerPoolWeight
   - Complex task → apply strongReasoningPoolWeight

**Preserves existing behavior**: When weights are 0 (default), no change to current scoring.

#### Failure / Fallback Considerations

**Current behavior** (from `chat.ts`, `chatCore.ts`, `combo.ts`):

- Circuit breaker per provider (OPEN/HALF_OPEN/CLOSED)
- Connection cooldown per account (`rateLimitedUntil`)
- Model lockout per provider+connection+model
- Fallback strategies: priority, weighted, round-robin, etc.
- Quality validation with retry

**Adaptive feature must**:

- Reuse existing fallback — role boost doesn't bypass circuit breaker/cooldown
- If role-assigned model unavailable, fall back to next highest scored candidate
- Never hard-fail due to role assignment

#### Five-Phase Implementation Plan

## Phase 1

Architecture validation and blueprint ✓ (this document)

## Phase 2

**Data/configuration + backend/runtime role support**

- Add `fastWorkerModelRefs`, `strongReasoningModelRefs` fields to `IntelligentRoutingConfig` type (arrays of step IDs)
- Update `normalizeIntelligentRoutingConfig()` to validate step ID references exist in combo's steps
- Extend `ScoringFactors` with `fastWorkerPoolSuitability`, `strongReasoningPoolSuitability`
- Extend `ScoringWeights` with `fastWorkerPoolWeight`, `strongReasoningPoolWeight` (default 0)
- Update `calculateScore()` to include role pool suitability factors
- Update `scoreAutoTargets()` / `calculateFactors()` to compute suitability from combo config
- Add lightweight task classification heuristic (message/token/tool analysis)
- Update combo API validation to accept new fields

**Files to modify**:

- `src/lib/combos/intelligentRouting.ts` — types, normalization
- `open-sse/services/autoCombo/scoring.ts` — factors, weights, calculateScore
- `open-sse/services/combo/autoStrategy.ts` — scoreAutoTargets, calculateFactors
- `src/app/api/combos/route.ts` — validation schema
- `src/shared/validation/schemas/combo.ts` — Zod schema

## Phase 3

**Combo Builder UI + Fast Worker / Strong Reasoning pool selectors**

- Extend `BuilderIntelligentStep.tsx` with two multi-select components
- Pass current Combo Steps (`models` from page.tsx) as options
- Use step `id` as value, display `model` + connection info as label
- Wire `onChange` to update `config.fastWorkerModelRefs` / `config.strongReasoningModelRefs`
- Add i18n keys for new labels
- Validate on Review step that referenced steps exist

**Files to modify**:

- `src/app/(dashboard)/dashboard/combos/BuilderIntelligentStep.tsx` — UI
- `src/app/(dashboard)/dashboard/combos/page.tsx` — pass models to BuilderIntelligentStep
- `config/i18n.json` / `config/i18n-schema.json` — translations

## Phase 4

**Adaptive analysis + role-pool-aware intelligent scoring/runtime selection**

- Implement task classification in `handleChatCore()` or new module
- Map task types to role pool preference (simple→Fast Worker pool, complex→Strong Reasoning pool)
- Integrate with scoring: activate appropriate role pool weight based on classification
- Mode pack integration: quality-first → higher strongReasoningPoolWeight, ship-fast → higher fastWorkerPoolWeight
- Add configuration UI for role pool weights (optional, in Advanced Weights)
- Implement intra-role failover (existing scorer handles) and cross-role failover logic

**Files to modify**:

- `open-sse/handlers/chatCore.ts` or new `taskClassification.ts` — classification
- `open-sse/services/autoCombo/scoring.ts` — weight activation logic
- `open-sse/services/combo/autoStrategy.ts` — pass task type to scoring, cross-role fallback
- `src/lib/combos/intelligentRouting.ts` — mode pack weight defaults

## Phase 5

**Integration tests, regression, performance, cleanup, documentation**

- Unit tests for task classification
- Unit tests for role pool suitability scoring
- Integration tests for combo builder save/load with role pools
- E2E test: create combo with role pools → verify routing prefers correct pool
- Performance benchmark: scoring overhead <1ms
- Update `docs/routing/AUTO-COMBO.md` with role pool assignment docs
- Update `docs/reference/API_REFERENCE.md` for config schema
- Cleanup: remove any debug logging, ensure lint/typecheck pass

**Test files**:

- `tests/unit/combos/adaptive-routing-roles.test.ts`
- `tests/unit/autoCombo/taskClassification.test.ts`
- `tests/integration/combos/builder-adaptive-roles.test.ts`

#### Risks

1. **Step ID stability**: Step IDs change if step is deleted/re-added. Role references may break. Mitigation: Validate on save, degrade gracefully at runtime.
2. **Task classification accuracy**: Heuristic may misclassify. Mitigation: Start conservative (low weights), allow operator tuning.
3. **Scoring complexity**: Adding factors increases cognitive load. Mitigation: Default weights = 0, opt-in via mode packs.
4. **Combo-ref steps**: Role assignment to a combo-ref step is ambiguous. Mitigation: Only allow model steps for roles (validate in UI).
5. **Virtual auto-combos**: Don't have fixed steps. Mitigation: Role assignment only applies to user-created combos with explicit steps.

#### Open Questions

1. Should role weights be configurable per-mode-pack or global?
2. Should task classification use the existing `getTaskFitness()` infrastructure or a new lightweight heuristic?
3. How to handle combo-ref steps in role assignment — disallow, or resolve to leaf models?
4. Should the UI show a warning when a role references a step that no longer exists?
5. Do we need a "neither" option (clear role assignment) in the dropdown?

### Phase 2 — Multi-Model Role Pools, Persistence & Runtime Foundation

Status: Completed

#### Scope Correction

The earlier Phase 2 outline above included task classification and scoring changes. That outline
is superseded by the final pre-Phase-2 architecture decision: Phase 2 is foundation-only.
Semantic difficulty analysis, role preference, and scoring changes remain Phase 4 work.

#### Implemented

- Added optional `fastWorkerModelRefs` and `strongReasoningModelRefs` arrays to the existing
  `IntelligentRoutingConfig`.
- Added strict API schema support for both arrays without requiring them on legacy Combos.
- Added role-pool normalization that trims and deduplicates refs, rejects non-string refs, and—
  when current normalized Combo Steps are available—removes stale, missing, and non-model refs.
- Preserved unrelated Combo runtime configuration during role-pool normalization.
- Wired normalization into both Combo creation and editing. Editing Steps also revalidates an
  existing role configuration, so deleting a Step removes its stale role reference on save.
- Added runtime resolution from configured Step IDs to already-eligible `ResolvedComboTarget`
  candidates. This preserves `stepId`, `executionKey`, and connection identity.
- Added a finite preferred-role → alternate-role → unassigned/existing-candidates fallback-order
  helper. It deduplicates by execution key and cannot recurse between roles.
- Exposed resolved role pools from `parseAutoConfig()` without changing scoring or target order.

#### Behavior Decisions

- Role members must be model Steps from the current Combo.
- The same Step may belong to both pools.
- Empty or absent pools are safe and preserve existing Intelligent Routing behavior.
- Role membership never restores a target removed by normal eligibility/resilience filtering.
- Complete role-pool exhaustion leaves the existing Combo candidates and fallback behavior intact.
- No provider/model names, credentials, or model capability inference were introduced.

#### Files Changed

- `src/lib/combos/intelligentRouting.ts`
- `src/shared/validation/schemas/combo.ts`
- `src/app/api/combos/route.ts`
- `src/app/api/combos/[id]/route.ts`
- `open-sse/services/combo/rolePools.ts`
- `open-sse/services/combo/autoConfig.ts`
- `open-sse/services/combo.ts`
- `tests/unit/combo-intelligent-role-pools.test.ts`

#### Validation

- Focused and adjacent unit tests: 62 passed, 0 failed.
- `npm run typecheck:core` equivalent with the repository's Node 24 runtime: passed.
- `npm run typecheck:noimplicit:core` equivalent with the repository's Node 24 runtime: passed.
- Targeted ESLint on all new/changed implementation files except the pre-existing suppressed
  `@/lib/localDb` import in `src/app/api/combos/route.ts`: passed.
- Full ESLint reached unrelated pre-existing `no-explicit-any` failures in
  `tests/unit/cli-oauth-commands.test.ts` and `tests/unit/executor-gitlab.test.ts` (25 errors total);
  none are in files changed for this feature.
- `git diff --check`: passed.

#### Current Limitation / Next Phase

The Combo Builder does not expose role-pool selectors yet. Phase 3 must add the two multi-select
controls sourced only from the current Combo's model Steps. No semantic role preference is active;
that remains Phase 4.

### Phase 3 — Combo Builder Role-Pool Selection UI

Status: Completed

#### Implemented

- Added Fast Worker Pool and Strong Reasoning Pool multi-select controls to the existing
  Combo Builder Intelligent / Smart Routing stage.
- Options are derived exclusively from normalized explicit model Steps in the current Combo.
  Combo references, provider wildcards, global model catalogs, and arbitrary model strings are not
  exposed as role options.
- Each option displays its qualified model plus pinned connection label/ID or automatic-connection
  status, so the same provider/model on different connections remains distinguishable.
- Both pools support zero to many selections, and the same Step can be selected in both pools.
- Added selected counts, empty-state guidance, and warnings for stale role references. Interacting
  with a pool removes stale refs; the Phase 2 save normalizer remains the final safety boundary.
- Added Fast Worker and Strong Reasoning membership summaries to the Builder Review stage.
- Added English and Indonesian UI messages. Other locales continue using the component's existing
  English fallback behavior when these new keys are absent.

#### Stable Draft Identity

New Builder Steps do not initially carry persisted IDs. The Builder now normalizes the current
draft before creating role options. On the first role-config change, those generated IDs are
materialized back into the draft Steps. Because `normalizeComboModels()` preserves explicit IDs,
role membership survives later Step reorder and Combo rename operations.

#### Files Changed

- `src/app/(dashboard)/dashboard/combos/BuilderIntelligentStep.tsx`
- `src/app/(dashboard)/dashboard/combos/page.tsx`
- `src/lib/combos/intelligentRouting.ts`
- `src/i18n/messages/en.json`
- `src/i18n/messages/id.json`
- `tests/unit/combo-intelligent-role-pools.test.ts`

#### Validation

- Focused and adjacent unit tests: 79 passed, 0 failed.
- `npm run typecheck:core` equivalent with Node 24: passed.
- `npm run typecheck:noimplicit:core` equivalent with Node 24: passed.
- Targeted ESLint with the repository suppression baseline: passed.
- `git diff --check`: passed.
- UI i18n dry-run completed; it also reports substantial pre-existing locale drift unrelated to
  this feature, so Phase 3 did not mass-edit 40+ locale catalogs with untranslated markers.

#### Current Limitation / Next Phase

The selected pools are persisted and visible but do not yet affect runtime ranking. Phase 4 must
add deterministic request analysis, choose a preferred role, and feed that preference into the
existing Intelligent Routing scorer while preserving eligibility and fallback authority.

### Phase 4 — Adaptive Analysis & Role-Aware Runtime Scoring

Status: Completed

#### Implemented

- Reused the existing synchronous multilingual intent classifier already executed by the Auto
  strategy, avoiding a second prompt-analysis pipeline or an LLM classification call.
- Added deterministic adaptive task classification:
  - `simple` intent without complex request signals prefers the Fast Worker pool.
  - `math` and `reasoning` intents prefer the Strong Reasoning pool.
  - Coding requests split into light, heavy, and neutral signals instead of always selecting Strong.
  - Explicit tool choice or an extracted request of at least 2,000 tokens promotes an otherwise
    ambiguous new request to Strong Reasoning.
  - Ambiguous `medium` and `creative` requests remain neutral unless a complexity signal exists.
- Added Fast Worker and Strong Reasoning suitability factors to the existing Auto-Combo scorer.
  They default to zero and therefore do not affect Combos without an active role preference.
- Added runtime role-weight activation that reserves a conservative portion of the existing
  normalized scoring distribution instead of increasing the total above 1.0.
- Integrated mode-pack bias: Ship Fast has the strongest Fast Worker bias, Quality First has the
  strongest Strong Reasoning bias, and Cost Saver moderately favors Fast Worker requests.
- Applied membership by stable Combo Step ID after quota/eligibility filtering. A role assignment
  cannot restore an unavailable candidate.
- Wired role-aware weights into both primary rules-engine selection and fallback scoring. The
  engine does not reapply a mode pack over the already-adjusted weights.
- Added finite cross-role fallback ordering: remaining preferred-role targets, alternate-role
  targets, then unassigned/ordinary candidates, deduplicated by execution key.
- Preserved explicit non-rules router selection as the primary authority while still using the
  adaptive role chain for its fallback tail.

#### Files Changed

- `open-sse/services/autoCombo/taskClassification.ts`
- `open-sse/services/autoCombo/scoring.ts`
- `open-sse/services/combo/resolveAutoStrategy.ts`
- `tests/unit/autoCombo/taskClassification.test.ts`

#### Validation

- Focused Node unit and runtime-ordering tests: 33 passed, 0 failed.
- Focused Vitest suites for adaptive classification and Auto-Combo scoring: 65 passed, 0 failed.
- `npm run typecheck:core`: passed.
- `npm run typecheck:noimplicit:core`: passed.
- Targeted ESLint and Prettier checks: passed.
- Full Vitest currently has one unrelated provider-family fixture drift: the GLM family now also
  discovers `cloudflare-playground`; all other suites passed.

#### Current Limitation / Next Phase

Phase 4 intentionally does not add manual role-weight sliders; mode packs activate conservative
runtime defaults automatically. Phase 5 owns builder save/load integration coverage, end-to-end
routing verification, performance measurement, public routing/API documentation, and cleanup.

### Phase 5 — Integration, Performance, Documentation & Regression Cleanup

Status: Completed

#### Implemented

- Added authenticated integration coverage through the real Combo create/list/update routes. It
  verifies stable Step references, overlapping role membership, and stale-reference pruning.
- Added a browser E2E flow that creates two model Steps, assigns Fast Worker and Strong Reasoning
  roles in the Builder, saves the Combo, and verifies the emitted Step-ID references.
- Fixed template-aware i18n fallback calls found by the browser run so translated messages receive
  their `count`, `percent`, and `strategy` values without client formatting errors.
- Added a 20,000-request microbenchmark covering adaptive classification and scoring across 20
  candidates, with a hard regression gate of less than 1 ms average per request.
- Updated Auto-Combo, API reference, and OpenAPI documentation for the two adaptive role fields,
  classification policy, normalized scoring, finite fallback behavior, and actual Combo methods.
- Updated the GLM provider-family fixture for the verified no-auth `cloudflare-playground`
  `zai-org/glm-5.2` catalog entry.

#### Files Added

- `tests/integration/adaptive-routing-roles.test.ts`
- `tests/integration/adaptive-routing-performance.test.ts`

#### Validation

- Focused integration tests: 2 passed, 0 failed.
- Focused Chromium Builder E2E: 1 passed, 0 failed.
- Adaptive performance gate: 0.0063 ms/request average across 20,000 requests × 20 candidates.
- Full Vitest: 375 passed, 0 failed.
- Core and no-implicit-any typechecks: passed.
- Targeted ESLint, Prettier, docs accuracy/link checks, OpenAPI checks, and `git diff --check`:
  passed. Documentation checks retain only their pre-existing non-fatal global drift warnings.

#### Phase Result

Adaptive Smart Routing is now covered from persisted API configuration through runtime scoring and
the user-facing Builder. No additional feature phase is required for the scoped implementation;
future work can add optional observability or operator-tunable role weights as separate features.

### Post-Phase 5 — Client-Agnostic Intent Extraction

Status: Completed

- Added a shared extractor that preserves ordinary chat messages and selects the last explicit
  human request from IDE envelopes (`userRequest`, `user_request`, `user-query`, and variants).
- Intent classification now ignores system/editor instructions, client metadata, memory blocks,
  and reminder text.
- Available tool schemas no longer force Strong Reasoning. Only explicit tool selection does.
- Removed accumulated message count as an adaptive-role trigger and measures long input from the
  extracted current user request.
- Added boundary-aware Latin keyword matching so fragments such as `api` in `capital` and `class`
  in `classification` do not produce code false positives; non-Latin substring matching remains.
- Added conversational Simple intent phrases including `hi`, `hey`, `how are you`, and
  `what can you do`.
- Added IDE, ordinary-chat, tool-choice, substring, runtime-wiring, and performance regressions.
- Validation: 30 focused Node tests, 8 focused Vitest tests, and full Vitest 376/376 passed;
  IDE-envelope classification averaged 0.0573 ms/request over 20,000 measured requests.
- Fixed cross-role epsilon exploration: rules-based primary selection now receives only routable
  candidates from the preferred role when that pool is available. Alternate and ordinary targets
  remain in the execution fallback tail. Selection logs include `role-pool=<role>` when scoped.

### Post-Phase 5 — Auto Primary Routing Authority

Status: Completed

- Added a pipeline regression reproducing the reported IDE request shape: 58 advertised tools,
  large client metadata, Lightning selected by Adaptive Auto, and Ultra preferred by the legacy
  `many-tools-large-context` classifier.
- Auto now remains the sole authority for its primary target. Legacy task-aware routing may reorder
  only the fallback tail, including when the standard rules router selected the primary.
- Prompt-cache affinity likewise preserves Auto's primary while retaining global affinity ordering
  across fallback targets and account candidates.
- Clarified that the six adaptive scoring items in the feature explanation are internal runtime
  steps, not six missing Combo Builder controls.

### Post-Phase 5 — Role-Specific Advanced Weights & Internal Field Sanitization

Status: Completed

- Added optional `fastWorkerWeights` and `strongReasoningWeights` profiles alongside the existing
  neutral/default `weights` profile.
- The Builder exposes Default, Fast Worker, and Strong Reasoning weight panels. Each role can
  inherit Default or enable its own persisted profile; the Review step shows the active state.
- A preferred role with one eligible model selects it directly. With multiple eligible models,
  Auto ranks only those models using that role's profile before building the cross-role fallback.
- Request-level mode overrides remain authoritative. Legacy task routing and prompt-cache affinity
  cannot replace Auto's primary selection.
- Fixed Context Handoff control leakage by stripping `_omnirouteInternalRequest` and
  `_omnirouteSkipContextRelay` at the universal outbound target boundary.
- Added unit coverage for normalization/schema/runtime ranking and the upstream payload regression,
  plus API integration coverage for create/list/update persistence.

#### Validation

- Focused Context Handoff and outbound-request tests: 46 passed, 0 failed.
- Focused Auto routing, role-profile, primary-authority, and affinity tests: 37 passed, 0 failed.
- Adaptive Combo API integration: 1 passed, 0 failed.
- `npm run typecheck:core`: passed.
- `npm run typecheck:noimplicit:core`: passed.
- Targeted ESLint and Prettier checks: passed.
- Full Vitest: 376 passed, 0 failed.
- Documentation accuracy, internal-link, and fabricated-reference gates: passed.
- Production build reached application compilation but was blocked by sandbox network access to
  Google Fonts (`Inter`), unrelated to the changed code. The dev-mode browser retry reached normal
  startup but its health-route cold compilation did not complete on the slow M: filesystem, so the
  new Playwright flow is present but this local E2E run did not reach its assertions.

### Post-Phase 5 — Optional AI Intent Classifier

Status: Completed

- Added optional `adaptiveJudgeModelRef` as one stable model Step ID. Arrays and arbitrary models
  outside the current Combo are rejected by the config contract/UI shape, while stale or non-model
  references are removed during create/update normalization.
- Added an AI Intent Classifier single-select populated exclusively from the current Combo's model
  Steps and a Review summary showing the selected classifier or rules-only mode.
- The judge receives only the extracted current user request in a small non-streaming request and
  must return exactly `FAST_WORKER` or `STRONG_REASONING`.
- A valid verdict supplies the role only after deterministic rules and neutral router scoring both
  remain role-neutral. The existing Auto scorer still chooses the concrete primary model inside the
  selected worker pool.
- Judge failures, timeouts, invalid output, or missing targets fail open to the neutral Auto path and
  never block the main request.
- Internal judge calls skip Context Relay and session/account-affinity tracking and use the exact
  selected Step target, including its pinned connection.
- Fixed classifier-only isolation: a selected classifier that is not checked in either worker pool is now
  excluded from Auto candidates, scoring, continuity/affinity reordering, final responses, and the
  fallback chain. A classifier explicitly checked in a worker pool may still intentionally serve both.

### Post-Phase 5 — General Workers, Neutral Judging, and Coding Complexity

Status: Completed

- Empty Fast Worker or Strong Reasoning assignments now fall back to the non-judge model Steps as a
  general worker pool instead of disabling adaptive scoring.
- The classified role's Advanced Weight profile ranks that general pool. A populated explicit role
  pool remains a hard primary-selection boundary.
- Legacy `task-route` and prompt-cache affinity may reorder only Auto's fallback tail. They cannot
  override the primary selected from either an explicit role pool or the general worker pool.
- AI Intent Classifier is not triggered merely by a neutral deterministic classification. Neutral
  requests first use local Auto router scoring; only a role-neutral top score can reach the optional
  classifier, while clear Fast Worker and Strong Reasoning requests bypass the extra model call.
- Added light-versus-heavy coding signals: routine formatting, renaming, lookup, and test-running
  prefer Fast Worker; debugging, architecture, refactoring, migrations, and repository-wide work
  prefer Strong Reasoning; ambiguous coding can be resolved by router scoring or the optional judge.
- Assistant/tool automation continuations ignore protocol-level explicit tool choice as a new
  complexity signal, and repeated identical judge-needed requests reuse a bounded one-hour verdict
  cache keyed by Combo, judge target, extracted prompt, and routing-context digest.

#### Validation

- Focused native role-pool, runtime selection, general-worker, judge isolation, and primary-authority
  regressions: 33 passed, 0 failed.
- Full Vitest: 382 passed, 0 failed, including deterministic classification, judge dispatch,
  fail-open parsing, and repeated-turn cache coverage.
- Core and no-implicit-any typechecks: passed.
- Targeted ESLint and Prettier checks: passed.
- Documentation accuracy, internal-link, OpenAPI, and fabricated-reference gates: passed; only the
  repository's pre-existing non-fatal global count/version drift warnings remain.
- The full native repository suite required an `os.userInfo()` sandbox shim to start, then reached
  an unrelated Adobe Firefly test that waited on a stale real browser risk session; it was stopped
  there. The routing-focused native suite above completed independently.

### Post-Phase 5 — Builder Classifier Polish

Status: Completed

- Renamed the user-facing `AI Judger` control to `AI Intent Classifier` (Indonesian:
  `Pengklasifikasi Intent AI`) while preserving the existing persisted field for compatibility.
- Clarified that the classifier resolves only ambiguous Fast-versus-Strong requests; deterministic
  decisions and classifier failures continue through rules.
- Added the missing gutter between Adaptive Model Roles and the classifier card, matching the
  spacing used before the Mode Pack / Router Strategy row.
- Updated the Review label and Builder E2E selector to use the new terminology.
- Corrected the two new native classifier tests to use the repository's Node test runner.
- Validation: 82 focused routing/integration tests passed; `npm run typecheck:core` passed.

#### Deferred UI Follow-up

- Make effective scoring weights visible. A selected Mode Pack currently becomes the base scoring
  profile, so editable Default / Neutral sliders can appear authoritative even when the preset is
  the effective source. Add an effective-profile preview or disable/explain shadowed controls.
- Rename the stale `Rules (6-Factor Scoring)` label because Auto scoring now contains more than six
  factors.

### Front Task Analysis & Context-Aware Classification

Status: Completed

- Added a shared front routing context that separates current-task complexity from technical
  capability requirements. Total input size, message count, and advertised tool count no longer
  promote an Auto request to Strong Reasoning by themselves.
- Added a bounded recent-work summary (maximum six meaningful events / 2,000 characters), recent
  tool activity and failure signals, normalized reasoning effort, and a stable context digest.
- Deterministic classification still prioritizes the extracted current request. Two recent failed
  tool events can promote an otherwise ambiguous fix/debug/continue automation turn.
- AI Intent Classifier calls receive the current request plus bounded execution context, not raw
  conversation history or tool schemas, only after local router scoring also remains role-neutral.
  Cache keys include the routing-context digest, preventing a stale Fast verdict from being reused
  after the execution state materially changes.
- Auto passes its final Fast/Strong decision to legacy fallback ordering. Legacy task routing keeps
  full input size for context-window fit, but raw history/tool inventory cannot independently change
  the reasoning tier selected by the front classifier.
- Added structured debug telemetry for current-request tokens, total-input tokens, message/tool
  counts, recent tool activity/failures, and the context digest. Auto selection logs now include the
  concrete classification signals.
- Task-aware routing now emits an INFO decision line even when Auto protects the selected primary.
  The line declares `scope=fallback-only`, the protected primary, the ordered fallback tail, task
  level/reasons, and conversation cache key; non-Auto task-aware routes use
  `scope=primary-and-fallback`.

#### Validation

- Expanded native routing/integration suite: 85 passed, 0 failed.
- The performance gate remained below 1 ms/request (0.0065 ms/request across 20,000 requests × 20
  candidates); IDE-envelope extraction averaged 0.0654 ms/request.
- Core TypeScript, targeted ESLint, Prettier, and diff checks passed.

### Post-Phase 5 — Role Weight Inheritance Reset

Status: Completed

- Fixed Combo Builder state merging so `Use Default` removes the selected role's persisted weight
  override instead of merging the previous custom profile back into state.
- Returning to inherited mode now hides the role sliders and restores the inheritance notice.
  Choosing `Customize` again starts from the current Default / Neutral profile rather than the
  discarded custom values.
- Added unit regression coverage for authoritative removal while preserving unrelated Combo config
  and the other role's independent custom profile. The existing browser flow now also asserts the
  reset, inherited UI, default slider restoration, and omitted save payload.

### Post-Phase 5 — Categorized Adaptive Fallback Pools

Status: Implemented; runtime validation pending

- Fixed the reported cross-pool fallback leak by making the adaptive fallback chain explicitly
  categorized. Each resolved target carries request-local worker membership and fallback-tier
  metadata; no provider/model capability inference is used.
- Fallback precedence is now authoritative and classifier-driven:
  - Strong Reasoning request: Strong Reasoning → Fast Worker → General/unassigned.
  - Fast Worker request: Fast Worker → Strong Reasoning → General/unassigned.
- `task-route` may still rank models by task fit, but only inside the current adaptive fallback
  tier. It cannot promote a later worker pool or General model ahead of an earlier tier.
- Prompt-cache affinity obeys the same tier boundary, preventing the post-task-routing cache stage
  from reintroducing cross-pool fallback jumps.
- General/unassigned models are the final safety pool. Overlapping Fast/Strong membership is
  deduplicated by execution key and consumed in the first applicable tier.
- Added focused regression coverage in
  `tests/unit/adaptive-task-routing-pool-boundaries.test.ts` for Strong→Fast→General,
  Fast→Strong→General, task-route tier isolation, and prompt-cache tier isolation.
- Task-routing INFO logs now expose categorized fallback models as
  `fallbackPools=<role>:[...] > <role>:[...] > general:[...]`; non-adaptive task-aware routes retain
  the legacy flat `fallbacks=...` format.

#### Validation

- Regression tests were added for the four pool-boundary behaviors above.
- The repository `Build App` GitHub workflow is configured for pushes to every branch, so these
  commits automatically request a production build on `codex/adaptive-routing`.
- This ChatGPT GitHub connection does not expose a command runner or workflow-dispatch/run-list API,
  so the Node/Vitest/typecheck/lint commands recorded elsewhere in this file have not been
  re-executed from this session yet. Do not treat this subsection as a green runtime validation
  until the branch build or a local checkout reports the results.

### Post-Phase 5 — Neutral Router-Score Gate for AI Intent Classifier

Status: Implemented; runtime validation pending

- Fixed the AI Intent Classifier trigger so a deterministic `neutral` result no longer causes an
  immediate model call.
- Neutral requests first reuse the normal Auto `scoreAutoTargets()` path with the effective base
  router weights and **no adaptive role boost**. This is a local scoring pass and consumes no model
  tokens.
- A unique top-scoring Fast Worker or Strong Reasoning Step resolves the role directly and records a
  `router-score:<role>` signal; the optional AI classifier is skipped.
- The AI classifier is now a last-resort role tie-breaker only when both Fast Worker and Strong
  Reasoning pools have routable candidates and the top router score remains role-neutral: a
  General/unassigned winner, a dual-role winner, or a Fast-versus-Strong score tie within the
  router-score epsilon.
- If only one or neither role pool has a routable candidate, the classifier is skipped because it
  cannot make a meaningful Fast-versus-Strong choice; normal neutral Auto scoring continues.
- The categorized execution contract is unchanged: Strong → Fast → General and Fast → Strong →
  General. `task-route` and prompt-cache affinity still cannot cross adaptive fallback tiers.
- Added `adaptiveRoleResolution.ts` plus focused unit coverage for unique Fast/Strong winners,
  cross-role ties, General winners, dual-role winners, and near-but-not-tied opposing scores.

#### Validation

- Source and regression tests are committed on `codex/adaptive-routing`.
- Runtime tests/typechecks/build have not been executed by this GitHub-editing session; run the
  focused Node test and repository validation locally before treating this change as fully green.
